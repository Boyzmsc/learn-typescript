declare const maybe: unknown;

// const aNum: number = maybe => Error

if (maybe === true) {
  const aBool: boolean = maybe;

  // const aStr: string = maybe => Error
}

if (typeof maybe === "string") {
  const aStr: string = maybe;

  // const aBool: boolean = maybe => Error
}
